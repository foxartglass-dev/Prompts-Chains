Quick project health check. Run these and report:

1. **Git Status**: `git status` - any uncommitted changes?
2. **Recent Commits**: `git log --oneline -5` - what happened recently?
3. **Current Branch**: which branch are we on?

Summarize the current state in 2-3 sentences. Don't start any work, just report status.
